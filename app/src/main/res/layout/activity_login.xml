<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".activity.LoginActivity">

    <Spinner
        android:id="@+id/class_spinner"
        android:layout_width="200dp"
        android:layout_height="30dp"
        android:layout_marginBottom="10dp"
        android:background="@drawable/edittext_round"
        android:spinnerMode="dialog"
        app:layout_constraintBottom_toTopOf="@+id/kakao_login_button"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

    <EditText
        android:id="@+id/userNickName_edit"
        android:layout_width="200dp"
        android:layout_height="40dp"
        android:hint="닉네임을 정해주세요"
        android:paddingLeft="10dp"
        android:textSize="14dp"
        android:fontFamily="@font/notosans_medium"
        android:layout_marginBottom="10dp"
        android:background="@drawable/edittext_round"
        app:layout_constraintBottom_toTopOf="@+id/userClassNum_edit"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

    <EditText
        android:id="@+id/userClassNum_edit"
        android:layout_width="200dp"
        android:layout_height="40dp"
        android:textSize="14dp"
        android:paddingLeft="10dp"
        android:hint="학번을 입력하세요"
        android:fontFamily="@font/notosans_medium"
        android:layout_marginBottom="10dp"
        android:background="@drawable/edittext_round"
        app:layout_constraintBottom_toTopOf="@+id/class_spinner"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

    <ImageButton
        android:id="@+id/kakao_login_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@android:color/transparent"
        android:src="@drawable/kakao_login_medium_narrow"
        android:layout_marginBottom="60dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

    <Button
        android:id="@+id/next_Btn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="NEXT"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>